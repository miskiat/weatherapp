import React from "react";

const Sky = ({ icon, title, desc }) => {
  return (
    <div>
      <img />
      <div>
        <div>{title}</div>
        <div>{desc}</div>
      </div>
    </div>
  );
};

export default Sky;
